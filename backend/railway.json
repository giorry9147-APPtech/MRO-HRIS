{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "bash -lc 'php artisan config:clear; php artisan route:clear; php artisan view:clear; php artisan migrate --force; php artisan db:seed --class=OrganogramSeeder --force; php artisan storage:link || true; php artisan serve --host=0.0.0.0 --port=${PORT:-8080}'",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}